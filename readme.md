Create build for a spacific package
```bash
npm run build --workspace=PACKAGE_NAME
```

Deploy a spacific package
```bash
npm run deploy --workspace=PACKAGE_NAME
```
Add new Plugin
```bash
npm run add-plugin
```
Then follow the steps